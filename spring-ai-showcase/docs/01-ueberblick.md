Erstelle bitte eine vollständige, lauffähige Spring-Boot-Beispiele zu den folgenden 6 Punkten der LLM-Integration:

1. Anbindung eines LLM per REST (OpenAI oder Azure OpenAI)
2. Prompt-Orchestrierung / Prompt-Templates
3. RAG (Retrieval Augmented Generation) mit Embeddings + Vektorsuche
4. Integration einer Vektordatenbank (z. B. Pinecone, Qdrant oder Azure AI Search)
5. Nutzung von LangChain4J in einem Spring-Boot-Service
6. KI-gestützte Business-Logik (z. B. Klassifikation oder Zusammenfassung)

Für jeden Punkt:
- Erstelle eine eigene Java-Klasse oder ein eigenes Modul.
- Nutze moderne Spring-Boot-Konventionen (WebClient, Services, Components).
- Verwende realistische Beispiel-Prompts.
- Erstelle sinnvolle Beispiel-Endpunkte (REST-Controller), wo es passt.
- Erkläre kurz, was jede Klasse macht.
- Nutze einfache, aber realistische Modelle (z. B. gpt-4o-mini).
- Code soll kompakt, aber vollständig sein.

Am Ende soll ein komplettes Mini-Projekt entstehen, das alle 6 Bereiche demonstriert.
